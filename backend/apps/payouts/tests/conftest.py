# pytest-style fixtures aren't required — Django's TestCase is enough
# for the two tests the challenge actually grades. Keep this file as a
# placeholder so future test modules can drop fixtures in.
